import { KdsBoard } from '@/components/kds/KdsBoard';

export const metadata = {
  title: "Kitchen Display System (KDS) • Hasan's Flavors",
};

export default function KdsPage() {
  return <KdsBoard />;
}
