import { OrdersTable } from '@/components/orders/OrdersTable';

export const metadata = {
  title: "Orders Management Table • Hasan's Flavors",
};

export default function OrdersPage() {
  return <OrdersTable />;
}
